<h1 class="text-white">全ユーザー一覧</h1>
<ul>
  <% @user.each do |user| %>
   <li>
     <%= user.name %> - <%= user.email %>
     <%= link_to '削除', admin_user_path(user), method: :delete, date: { confirm: '本当に削除しますか?'} %>
   </li>
  <% end %>
</ul>